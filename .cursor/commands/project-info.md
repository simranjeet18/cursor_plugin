# Project Info

Show metadata about the current project.

Steps:
1. Call the `get_project_info` MCP tool for the project root.
2. Present: name, path, language, frameworks, package manager, detected source
   directory, git branch / last commit / dirty state, and knowledge-graph status
   (whether it exists, when it was generated, files + symbols indexed).
