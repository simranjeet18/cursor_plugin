# Recent Projects

List projects I've recently loaded.

Steps:
1. Call the `list_recent_projects` MCP tool.
2. Show a numbered list (most recent first) with name, path, language,
   frameworks, last-accessed time, and whether each has a knowledge graph.
3. If I pick a number, call `load_project` on that path.
