"""Cursor Project Manager — deterministic code knowledge graph for Cursor.

Exposes an MCP server that indexes Python projects into a structured knowledge
graph so any Cursor model can navigate code by exact file + line range without
blindly scanning files.
"""

__version__ = "0.1.0"
